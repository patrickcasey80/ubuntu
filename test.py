#### named test during build, need to give real name and update presort.py with new name

class address():
	def __init__(self,company,name,address1,address2,city,state,postalcode,country):

		self.company = company,
		self.fullname = name,
		self.address1 = address1,
		self.address2 = address2,
		self.city = city,
		self.state = state,
		self.zipcode = postalcode,
		self.country = country
